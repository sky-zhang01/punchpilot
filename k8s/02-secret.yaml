apiVersion: v1
kind: Secret
metadata:
  name: app-env
  namespace: punchpilot
type: Opaque
stringData:
  # Replace with your actual credentials
  .env: |
    LOGIN_USERNAME=your_username@example.com
    LOGIN_PASSWORD=your_password
    TZ=Asia/Tokyo
