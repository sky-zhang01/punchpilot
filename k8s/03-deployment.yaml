apiVersion: apps/v1
kind: Deployment
metadata:
  name: punchpilot
  namespace: punchpilot
  labels:
    app: punchpilot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: punchpilot
  template:
    metadata:
      labels:
        app: punchpilot
    spec:
      containers:
        - name: app
          image: ghcr.io/sky-zhang01/punchpilot:latest
          # Use IfNotPresent to avoid downloading the large image on every restart
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8681
          # Recommended resource limits to help scheduler and avoid eviction
          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          volumeMounts:
            - name: env-file
              mountPath: /app/.env
              subPath: .env
              readOnly: true
            - name: logs
              mountPath: /var/log/punchpilot
            - name: screenshots
              mountPath: /app/screenshots
      volumes:
        - name: env-file
          secret:
            secretName: app-env
        - name: logs
          persistentVolumeClaim:
            claimName: logs-pvc
        - name: screenshots
          persistentVolumeClaim:
            claimName: screenshots-pvc
